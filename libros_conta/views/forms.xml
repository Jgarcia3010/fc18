<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- COMENTAR la primera herencia problemática -->
    <!--
    <record id="view_res_partner_bank_form_inherited" model="ir.ui.view">
        <field name="name">res.partner.bank.inherited</field>
        <field name="model">res.partner.bank</field>
        <field name="inherit_id" ref="base.view_partner_bank_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='acc_type']" position="after">
                <field name="account_type" />
            </xpath>
        </field>
    </record>
    -->

    <!-- MANTENER la segunda herencia con SINTAXIS CORRECTA para Odoo 18 -->
    <record id="view_account_move_libros_extensions" model="ir.ui.view">
        <field name="name">account.move.libros_extensions</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_reference']" position="after">
                <field name="anticipo" invisible="move_type != 'out_invoice'" />
            </xpath>
        </field>
    </record>
</odoo>